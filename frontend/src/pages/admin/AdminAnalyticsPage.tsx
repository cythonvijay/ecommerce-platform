export default function AdminAnalyticsPage() {
  return (
    <div>
      <h1 className="font-display mb-3 text-2xl font-bold text-ink-900 dark:text-white">Analytics</h1>
      <p className="text-sm text-ink-500 dark:text-ink-400">
        Deep analytics (sales trends, cohort reports, etc.) are deferred to a follow-up step. The Dashboard page
        already shows the core KPIs (revenue, orders, low stock, recent activity).
      </p>
    </div>
  );
}
