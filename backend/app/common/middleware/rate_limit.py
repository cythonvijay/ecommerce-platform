"""Rate limiting is deferred to the infra layer (nginx/API gateway) for the
local-dev build. Placeholder kept so the module structure survives the future
microservices split without a Redis dependency in local dev."""
